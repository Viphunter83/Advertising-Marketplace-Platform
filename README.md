# 🏢 Advertising Marketplace Platform - MVP

Платформа-маркетплейс для размещения рекламы маркетплейсов в социальных каналах.

## 📋 Описание

Двусторонний B2B сервис, который соединяет:
- **Продавцов маркетплейсов** (Wildberries, Ozon, Яндекс.Маркет) — нужна реклама
- **Владельцев каналов/пабликов** (ВК, Telegram, Pinterest, Instagram, TikTok) — монетизация аудитории

## 🛠 Технологический стек

- **Backend**: Python 3.10.11, FastAPI
- **База данных**: Supabase (PostgreSQL)
- **ORM**: SQLAlchemy
- **Валидация**: Pydantic V2
- **Аутентификация**: JWT
- **AI**: ProxyAPI (ChatGPT 4o-mini)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Обязательно укажите:
- `SUPABASE_URL` — URL вашего Supabase проекта
- `SUPABASE_KEY` — anon public key
- `SUPABASE_SERVICE_ROLE_KEY` — service role key
- `JWT_SECRET_KEY` — секретный ключ для JWT токенов

### 3. Создание таблиц в Supabase

Выполните SQL миграции из файла `supabase_migrations.sql` в SQL Editor вашего Supabase проекта.

### 4. Запуск приложения

```bash
uvicorn main:app --reload
```

Приложение будет доступно по адресу:
- API: http://localhost:8000
- Swagger документация: http://localhost:8000/docs
- ReDoc документация: http://localhost:8000/redoc

## 📁 Структура проекта

```
advertising-marketplace/
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
├── .env.example            # Пример переменных окружения
├── supabase_migrations.sql # SQL миграции
├── app/
│   ├── config.py           # Конфигурация
│   ├── core/               # Безопасность, БД, зависимости
│   ├── models/             # SQLAlchemy модели
│   ├── schemas/            # Pydantic схемы
│   ├── services/           # Бизнес-логика
│   ├── routers/            # API маршруты
│   └── utils/              # Вспомогательные функции
└── README.md
```

## 🔐 API Endpoints

### Аутентификация
- `POST /auth/register` — регистрация
- `POST /auth/login` — вход

### Проверка работоспособности
- `GET /` — приветственное сообщение
- `GET /health` — проверка статуса приложения и БД

Полная документация доступна в Swagger UI: http://localhost:8000/docs

## 📝 Лицензия

Проект находится в разработке (MVP фаза).

