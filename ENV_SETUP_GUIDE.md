# 🔧 РУКОВОДСТВО ПО НАСТРОЙКЕ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

## ✅ Зависимости установлены!

Все зависимости Phase 2 успешно установлены. Теперь нужно настроить переменные окружения.

---

## 📧 1. SENDGRID_API_KEY (для Email-уведомлений)

### Что это?
API ключ от SendGrid для отправки email-уведомлений пользователям.

### Как получить:

1. **Зарегистрируйтесь на SendGrid:**
   - Перейдите на https://app.sendgrid.com/
   - Создайте бесплатный аккаунт (100 писем в день бесплатно)

2. **Создайте API ключ:**
   - Войдите в SendGrid Dashboard
   - Перейдите в **Settings** → **API Keys**
   - Нажмите **Create API Key**
   - Выберите **Full Access** или **Restricted Access** (рекомендуется для безопасности)
   - Скопируйте созданный ключ (он начинается с `SG.`)

3. **Добавьте в `.env`:**
   ```env
   SENDGRID_API_KEY=SG.your_actual_api_key_here
   ```

### Дополнительные настройки SendGrid:

- **SENDGRID_FROM_EMAIL** - Email адрес отправителя (должен быть верифицирован в SendGrid)
- **SENDGRID_FROM_NAME** - Имя отправителя (например, "Advertising Marketplace")

**Важно:** Для production нужно верифицировать домен или email адрес в SendGrid.

**Документация:** https://app.sendgrid.com/guide/integrate

---

## 🌐 2. WEBSOCKET_CORS_ORIGINS (для WebSocket)

### Что это?
Список разрешённых доменов, которые могут подключаться к WebSocket серверу. Это важно для безопасности.

### Как настроить:

**Для локальной разработки:**
```env
WEBSOCKET_CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000
```

**Для production:**
```env
WEBSOCKET_CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com
```

**Для разработки (разрешить все):**
```env
WEBSOCKET_CORS_ORIGINS=*
```

### Примеры:

1. **React/Vite на localhost:3000:**
   ```env
   WEBSOCKET_CORS_ORIGINS=http://localhost:3000
   ```

2. **Next.js на localhost:3000:**
   ```env
   WEBSOCKET_CORS_ORIGINS=http://localhost:3000
   ```

3. **Несколько доменов (через запятую):**
   ```env
   WEBSOCKET_CORS_ORIGINS=http://localhost:3000,https://staging.example.com,https://example.com
   ```

4. **Production с несколькими доменами:**
   ```env
   WEBSOCKET_CORS_ORIGINS=https://app.example.com,https://www.example.com
   ```

**Важно:** 
- Не используйте `*` в production!
- Указывайте точные домены с протоколом (http:// или https://)
- Разделяйте несколько доменов запятой (без пробелов)

---

## 🔗 3. FRONTEND_URL (для Email ссылок)

### Что это?
URL вашего фронтенд приложения. Используется в email-уведомлениях для создания ссылок (например, "Войти в личный кабинет").

### Как настроить:

**Для локальной разработки:**
```env
FRONTEND_URL=http://localhost:3000
```

**Для staging:**
```env
FRONTEND_URL=https://staging.your-domain.com
```

**Для production:**
```env
FRONTEND_URL=https://your-domain.com
```

### Примеры использования:

В email-уведомлениях будут создаваться ссылки вида:
- `http://localhost:3000/login` (для локальной разработки)
- `https://your-domain.com/seller/campaigns/123` (для production)

**Важно:** 
- Указывайте полный URL с протоколом
- Не добавляйте слэш в конце (`/`)
- Убедитесь, что URL доступен и работает

---

## 📝 ПОЛНЫЙ ПРИМЕР `.env` ФАЙЛА

```env
# ========== ОСНОВНЫЕ НАСТРОЙКИ ==========
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_anon_public_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_key

JWT_SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ========== PHASE 2: EMAIL (SendGrid) ==========
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=noreply@advertising-marketplace.com
SENDGRID_FROM_NAME=Advertising Marketplace

# ========== PHASE 2: WEBSOCKET ==========
WEBSOCKET_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ========== FRONTEND URL ==========
FRONTEND_URL=http://localhost:3000

# ========== ОСТАЛЬНЫЕ НАСТРОЙКИ ==========
FASTAPI_ENV=development
FASTAPI_DEBUG=true
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000

APP_NAME=Advertising Marketplace MVP
PLATFORM_COMMISSION_PERCENT=10

# ... остальные переменные из .env.example
```

---

## ✅ ПРОВЕРКА НАСТРОЙКИ

После настройки переменных окружения:

1. **Проверьте, что файл `.env` существует:**
   ```bash
   ls -la .env
   ```

2. **Убедитесь, что все переменные заполнены:**
   ```bash
   cat .env | grep -E "(SENDGRID_API_KEY|WEBSOCKET_CORS_ORIGINS|FRONTEND_URL)"
   ```

3. **Запустите приложение:**
   ```bash
   python main.py
   ```

4. **Проверьте логи:**
   - Если SendGrid API key не настроен, вы увидите предупреждение, но приложение будет работать
   - WebSocket будет работать с любыми CORS origins (включая `*` для разработки)

---

## 🚨 ВАЖНЫЕ ЗАМЕЧАНИЯ

### Безопасность:

1. **Никогда не коммитьте `.env` в Git!**
   - Файл `.env` уже должен быть в `.gitignore`

2. **Для production:**
   - Используйте конкретные CORS origins (не `*`)
   - Используйте сильный `JWT_SECRET_KEY`
   - Верифицируйте домен в SendGrid

3. **SendGrid:**
   - Бесплатный план: 100 писем/день
   - Для production рассмотрите платный план

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **SendGrid Documentation:** https://app.sendgrid.com/guide/integrate
- **SendGrid API Keys:** https://app.sendgrid.com/settings/api_keys
- **CORS Documentation:** https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS

---

## 🎯 БЫСТРЫЙ СТАРТ

Минимальная настройка для начала работы:

```env
# Минимальная настройка для локальной разработки
SENDGRID_API_KEY=SG.your_key_here
WEBSOCKET_CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000
```

После этого можно запускать приложение и тестировать функциональность!

