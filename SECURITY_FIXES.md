# 🔒 Исправления проблем безопасности

## ✅ Исправлено автоматически

### 1. Функция `update_updated_at_column` - search_path
**Проблема**: Функция имела `role mutable search_path`, что является уязвимостью безопасности.

**Решение**: ✅ Исправлено
- Добавлен `SET search_path = public` в определение функции
- Функция теперь безопасна и не подвержена атакам через изменение search_path

```sql
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER 
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$;
```

---

## ⚠️ Требуют действий в Supabase Dashboard

### 2. Защита от скомпрометированных паролей (HaveIBeenPwned)

**Проблема**: Отключена проверка паролей через HaveIBeenPwned.org

**Решение**: Включить в Supabase Dashboard

**Инструкция**:
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `Viphunter83's Project`
3. Перейдите в **Authentication** → **Policies** или **Settings**
4. Найдите раздел **Password Security** или **Password Strength**
5. Включите опцию **"Leaked Password Protection"** или **"Check passwords against HaveIBeenPwned"**
6. Сохраните изменения

**Ссылка на документацию**: https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

**Важно**: Эта функция проверяет пароли пользователей против базы данных HaveIBeenPwned.org, чтобы предотвратить использование скомпрометированных паролей.

---

### 3. Многофакторная аутентификация (MFA)

**Проблема**: Слишком мало опций MFA включено

**Решение**: Включить дополнительные методы MFA

**Инструкция**:
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `Viphunter83's Project`
3. Перейдите в **Authentication** → **Settings** → **Multi-Factor Authentication**
4. Включите следующие методы:
   - ✅ **TOTP (Time-based One-Time Password)** - через приложения типа Google Authenticator
   - ✅ **SMS** (если доступно)
   - ✅ **Email** (если доступно)
5. Настройте политики MFA:
   - Определите, для каких операций требуется MFA
   - Настройте обязательность MFA для администраторов
6. Сохраните изменения

**Ссылка на документацию**: https://supabase.com/docs/guides/auth/auth-mfa

**Рекомендации**:
- Для MVP можно начать с TOTP (самый популярный и безопасный метод)
- Для production рекомендуется включить хотя бы 2 метода MFA
- Обязателен MFA для администраторов и владельцев каналов (высокий уровень доступа)

---

### 4. Обновление PostgreSQL

**Проблема**: Текущая версия PostgreSQL (17.4.1.074) имеет доступные патчи безопасности

**Решение**: Обновить базу данных до последней версии

**Инструкция**:
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект `Viphunter83's Project`
3. Перейдите в **Settings** → **Database** или **Infrastructure**
4. Найдите раздел **Database Version** или **Upgrade Database**
5. Проверьте доступные обновления
6. Если доступно обновление:
   - **Важно**: Создайте резервную копию базы данных перед обновлением
   - Запланируйте обновление на время минимальной нагрузки
   - Обновление может занять несколько минут и потребовать перезапуска
7. Нажмите **Upgrade** или **Update Database**

**Ссылка на документацию**: https://supabase.com/docs/guides/platform/upgrading

**Важно**:
- Обновление обычно безопасно, но рекомендуется:
  - Создать резервную копию
  - Протестировать на staging окружении (если есть)
  - Обновлять в нерабочее время
  - Уведомить пользователей о возможном кратковременном простое

---

## 📋 Чеклист безопасности

После выполнения всех действий проверьте:

- [x] Функция `update_updated_at_column` исправлена (search_path)
- [ ] HaveIBeenPwned защита включена
- [ ] MFA настроен (минимум TOTP)
- [ ] PostgreSQL обновлен до последней версии
- [ ] Service Role Key получен и добавлен в `.env`
- [ ] `.env` файл не коммитится в Git (проверьте `.gitignore`)

---

## 🔍 Проверка исправлений

После выполнения всех действий проверьте статус через Supabase Advisors:

1. Откройте Supabase Dashboard
2. Перейдите в **Advisors** или **Security**
3. Проверьте, что все предупреждения устранены

Или используйте MCP команду:
```python
from mcp_supabase import get_advisors
advisors = get_advisors(project_id="zyabiuahahndthqzyzne", type="security")
```

---

## 📚 Дополнительные рекомендации по безопасности

### Row Level Security (RLS)
Для production рекомендуется включить RLS на всех таблицах:
- `users` - пользователи видят только свои данные
- `sellers` - продавцы видят только свои профили
- `channels` - владельцы видят только свои каналы
- `campaigns` - доступ только для участников заявки
- `transactions` - доступ только для участников транзакции

### API Keys
- Никогда не коммитьте `.env` файл
- Используйте разные ключи для development и production
- Регулярно ротируйте ключи
- Используйте Service Role Key только на backend, никогда на frontend

### JWT Tokens
- Используйте короткое время жизни токенов (24 часа - разумно)
- Реализуйте refresh tokens для долгосрочной сессии
- Валидируйте токены на каждом запросе

### Rate Limiting
- Настройте rate limiting для API endpoints
- Особенно для `/auth/login` и `/auth/register`
- Защита от brute-force атак

---

## ✅ Статус

- **Функция search_path**: ✅ Исправлено
- **HaveIBeenPwned**: ⏳ Требует действий в Dashboard
- **MFA**: ⏳ Требует действий в Dashboard  
- **PostgreSQL Update**: ⏳ Требует действий в Dashboard

