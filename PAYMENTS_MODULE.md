# 💰 Модуль платежей и финансов - Документация

## ✅ Реализовано

Модуль платежей и финансов реализует полную систему управления денежными потоками платформы.

### Таблицы в Supabase

1. **transactions** — все финансовые транзакции
2. **payment_methods** — сохранённые методы оплаты пользователей
3. **withdrawal_requests** — запросы на вывод средств

### API Endpoints

#### Для продавцов (требуют авторизации, тип: seller)

1. **POST /payments/deposit** — Пополнить баланс
   - Создаёт платёжную ссылку
   - Возвращает URL для оплаты

2. **GET /payments/balance** — Получить текущий баланс
3. **GET /payments/history** — История транзакций

#### Для владельцев каналов (требуют авторизации, тип: channel_owner)

1. **POST /payments/withdrawal** — Запросить вывод средств
   - Создаёт запрос на вывод
   - Администратор должен одобрить

2. **GET /payments/balance** — Получить баланс и доступные средства
3. **GET /payments/history** — История транзакций

#### Webhooks (для платёжных провайдеров)

1. **POST /payments/yoomoney/callback** — Webhook от YooMoney
2. **POST /payments/sbp/callback** — Webhook от СБП

## 🔄 Workflow платежей

### 1. Пополнение баланса продавца

```
1. Продавец: POST /payments/deposit
   {
     "amount": 10000,
     "payment_method": "yoomoney"
   }

2. API возвращает:
   {
     "transaction_id": "123...",
     "payment_url": "https://yoomoney.ru/quickpay/...",
     "amount": 10000,
     "status": "pending"
   }

3. Продавец переходит по payment_url и платит

4. YooMoney отправляет webhook:
   POST /payments/yoomoney/callback

5. Система вызывает:
   PaymentService.complete_deposit_transaction()

6. Баланс продавца увеличивается на 10000

7. Транзакция получает статус "completed"
```

### 2. Вывод средств владельца канала

```
1. Владелец канала: POST /payments/withdrawal
   {
     "amount": 10000,
     "method": "yoomoney",
     "account": "79991234567"
   }

2. Система создаёт запрос со статусом "pending"

3. Администратор проверяет и одобряет запрос
   (отдельный админ-endpoint для Phase 2)

4. Средства переводятся на указанный счёт

5. Запрос получает статус "completed"
```

### 3. ESCROW система (интегрирована с campaigns)

```
1. Продавец создаёт заявку:
   POST /campaigns/
   → Баланс продавца снижается (блокировка на escrow)

2. При подтверждении размещения:
   POST /campaigns/{id}/confirm
   → Создаются транзакции:
     - Commission (комиссия платформы)
     - Payment (выплата владельцу канала)
```

## 📊 Типы транзакций

| Тип | Описание | Кто создаёт |
|-----|----------|-------------|
| `deposit` | Пополнение баланса продавца | PaymentService |
| `withdrawal` | Вывод средств владельца канала | PaymentService |
| `commission` | Комиссия платформы | CampaignService |
| `payment` | Выплата владельцу канала | CampaignService |

## 💳 Методы оплаты

- **yoomoney** — ЮMoney (кошелёк)
- **sbp** — Система Быстрых Платежей
- **card_mir** — Карта МИР
- **qiwi** — QIWI кошелёк
- **bank_transfer** — Банковский перевод

## 🔒 Безопасность

- Валидация сумм (мин/макс лимиты)
- Проверка баланса перед операциями
- Маскирование номеров счетов
- Логирование всех транзакций
- Webhook signature verification (в production)

## 📈 Примеры использования

### Пополнение баланса

```bash
curl -X POST "http://localhost:8000/payments/deposit" \
  -H "Authorization: Bearer SELLER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 10000,
    "payment_method": "yoomoney"
  }'
```

**Ответ:**
```json
{
  "transaction_id": "123e4567-e89b-12d3-a456-426614174000",
  "payment_url": "https://yoomoney.ru/quickpay/...",
  "amount": 10000,
  "status": "pending",
  "message": "Click the link to complete payment"
}
```

### Получение баланса

```bash
curl "http://localhost:8000/payments/balance" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Ответ для продавца:**
```json
{
  "balance": 50000,
  "pending_withdrawals": 0,
  "available_balance": 50000,
  "currency": "RUB"
}
```

**Ответ для владельца канала:**
```json
{
  "balance": 125000,
  "pending_withdrawals": 10000,
  "available_balance": 115000,
  "currency": "RUB"
}
```

### История транзакций

```bash
curl "http://localhost:8000/payments/history?page=1&limit=20" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Ответ:**
```json
{
  "transactions": [
    {
      "id": "123...",
      "transaction_type": "deposit",
      "status": "completed",
      "amount": 10000,
      "commission": 0,
      "net_amount": 10000,
      "payment_method": "yoomoney",
      "description": "Deposit via yoomoney",
      "created_at": "2025-11-15T10:30:00Z",
      "completed_at": "2025-11-15T10:35:00Z"
    }
  ],
  "total_count": 15,
  "page": 1,
  "page_size": 20
}
```

### Запрос на вывод

```bash
curl -X POST "http://localhost:8000/payments/withdrawal" \
  -H "Authorization: Bearer OWNER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 10000,
    "method": "yoomoney",
    "account": "79991234567"
  }'
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# Платёжные системы
YOOMONEY_WALLET_NUMBER=410012345678901
YOOMONEY_API_KEY=your_yoomoney_api_key_here
YOOMONEY_REDIRECT_URL=http://localhost:8000/payments/yoomoney/callback

SBP_BANK_ID=100000000111
SBP_MERCHANT_ID=your_merchant_id
SBP_API_KEY=your_sbp_api_key

# Финансовые параметры
MIN_DEPOSIT_AMOUNT=100
MAX_DEPOSIT_AMOUNT=500000
MIN_WITHDRAWAL_AMOUNT=100
MAX_WITHDRAWAL_AMOUNT=500000

WEBHOOK_SECRET=your-webhook-secret-key
```

## 🔍 Интеграция с Campaigns

Модуль платежей тесно интегрирован с модулем заявок:

1. **При создании заявки**: Средства блокируются (вычитаются с баланса продавца)
2. **При подтверждении размещения**: 
   - Создаётся транзакция комиссии платформы
   - Создаётся транзакция выплаты владельцу канала
   - Средства добавляются на счёт владельца канала

## ✅ Готово к использованию

Модуль платежей полностью реализован и готов к использованию!

**Основные возможности:**
- ✅ Пополнение баланса через YooMoney и СБП
- ✅ Вывод средств владельцами каналов
- ✅ Полный аудит всех транзакций
- ✅ История финансовых операций
- ✅ Интеграция с ESCROW системой
- ✅ Webhook endpoints для платёжных провайдеров
- ✅ Валидация сумм и проверка балансов

