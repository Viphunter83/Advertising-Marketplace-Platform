# 📋 Модуль заявок (Campaigns) - Документация

## ✅ Реализовано

Модуль заявок — это ядро платформы, реализующее полный workflow размещения рекламы с ESCROW-системой.

### Таблицы в Supabase

1. **campaigns** — основная таблица заявок
2. **campaign_activities** — логирование всех действий по заявкам
3. **campaign_disputes** — споры по заявкам

### API Endpoints

#### Для продавцов (требуют авторизации, тип: seller)

1. **POST /campaigns/** — Создать заявку на размещение
   - Блокирует средства на escrow-счёте
   - Статус: `pending`

2. **GET /campaigns/my-campaigns** — Получить список своих заявок
   - Поддерживает фильтр по статусу: `?status_filter=pending`

3. **PUT /campaigns/{campaign_id}** — Обновить заявку
   - Только в статусе `pending`
   - Только создатель может редактировать

4. **POST /campaigns/{campaign_id}/confirm** — Подтвердить размещение
   - Если `confirmed=true`: выплата владельцу канала
   - Если `confirmed=false`: открывается спор

5. **POST /campaigns/{campaign_id}/cancel** — Отменить заявку
   - Только в статусе `pending`
   - Возвращает заблокированные средства

#### Для владельцев каналов (требуют авторизации, тип: channel_owner)

1. **GET /campaigns/my-campaigns** — Получить список заявок на свои каналы

2. **POST /campaigns/{campaign_id}/accept** — Принять заявку
   - Статус: `pending` → `accepted`

3. **POST /campaigns/{campaign_id}/reject** — Отклонить заявку
   - Возвращает заблокированные средства продавцу
   - Статус: `pending` → `rejected`

4. **POST /campaigns/{campaign_id}/submit** — Отметить размещение выполненным
   - Загружает доказательство размещения
   - Статус: `accepted` → `in_progress`

#### Общие endpoints

1. **GET /campaigns/{campaign_id}** — Получить информацию о заявке
2. **GET /campaigns/stats/my-stats** — Статистика по заявкам

## 🔄 Workflow заявки

### 1. Создание (Продавец)

```
POST /campaigns/
{
  "channel_id": "...",
  "start_date": "2025-11-20",
  "end_date": "2025-11-22",
  "budget": 5000.00,
  "ad_format": "post",
  "creative_text": "Текст рекламы...",
  "creative_images": ["https://..."],
  "seller_notes": "Примечания"
}
```

**Что происходит:**
- Проверяется баланс продавца
- Средства блокируются на escrow (вычитаются с баланса)
- Создаётся заявка со статусом `pending`
- Владелец канала получает уведомление

### 2. Согласование (Владелец канала)

**Принять:**
```
POST /campaigns/{campaign_id}/accept
{
  "owner_notes": "Размещу в понедельник"
}
```
Статус: `pending` → `accepted`

**Отклонить:**
```
POST /campaigns/{campaign_id}/reject
{
  "reason": "Не подходит тематика канала"
}
```
Статус: `pending` → `rejected`
**Средства возвращаются продавцу**

### 3. Размещение (Владелец канала)

```
POST /campaigns/{campaign_id}/submit
{
  "placement_proof_url": "https://vk.com/post/12345",
  "placement_proof_type": "post_link",
  "owner_notes": "Размещено успешно"
}
```
Статус: `accepted` → `in_progress`

### 4. Подтверждение (Продавец)

**Подтвердить:**
```
POST /campaigns/{campaign_id}/confirm
{
  "confirmed": true
}
```
Статус: `in_progress` → `completed`

**Что происходит:**
- Платформа вычитает комиссию (10% по умолчанию)
- Остаток (90%) переходит владельцу канала
- Обновляется статистика канала

**Открыть спор:**
```
POST /campaigns/{campaign_id}/confirm
{
  "confirmed": false,
  "dispute_reason": "Размещение не соответствует требованиям"
}
```
Статус: `in_progress` → `disputed`
**Администратор разберётся**

## 💰 ESCROW система

### Блокировка средств

При создании заявки:
- Средства вычитаются с баланса продавца
- Хранятся на escrow-счёте (виртуально)
- Не доступны ни продавцу, ни владельцу канала

### Возврат средств

Средства возвращаются продавцу при:
- Отклонении заявки владельцем канала
- Отмене заявки продавцом (только в статусе `pending`)

### Выплата владельцу канала

При подтверждении размещения:
- Комиссия платформы: `budget × 10%`
- Выплата владельцу: `budget × 90%`
- Средства добавляются на счёт владельца канала

## 📊 Статусы заявки

| Статус | Описание | Кто может изменить |
|--------|----------|-------------------|
| `pending` | Ожидает ответа владельца | Владелец канала (accept/reject) |
| `accepted` | Принята владельцем | Владелец канала (submit) |
| `rejected` | Отклонена | - |
| `in_progress` | Размещение выполнено | Продавец (confirm) |
| `completed` | Завершена, выплачена | - |
| `disputed` | В споре | Администратор |
| `cancelled` | Отменена продавцом | - |

## 🔍 Примеры использования

### Создание заявки

```bash
curl -X POST "http://localhost:8000/campaigns/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "channel_id": "323e4567-e89b-12d3-a456-426614174000",
    "start_date": "2025-11-20",
    "end_date": "2025-11-22",
    "budget": 5000.00,
    "ad_format": "post",
    "creative_text": "Проверьте нашу новую коллекцию!",
    "creative_images": ["https://example.com/img1.jpg"],
    "seller_notes": "Предпочитаю размещение в понедельник"
  }'
```

### Получение списка заявок

```bash
# Все заявки
curl "http://localhost:8000/campaigns/my-campaigns" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Только pending
curl "http://localhost:8000/campaigns/my-campaigns?status_filter=pending" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Принятие заявки

```bash
curl -X POST "http://localhost:8000/campaigns/{campaign_id}/accept" \
  -H "Authorization: Bearer OWNER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "owner_notes": "Размещу в понедельник утром"
  }'
```

### Подтверждение размещения

```bash
curl -X POST "http://localhost:8000/campaigns/{campaign_id}/submit" \
  -H "Authorization: Bearer OWNER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "placement_proof_url": "https://vk.com/post/12345",
    "placement_proof_type": "post_link",
    "owner_notes": "Размещено успешно"
  }'
```

### Подтверждение продавцом

```bash
curl -X POST "http://localhost:8000/campaigns/{campaign_id}/confirm" \
  -H "Authorization: Bearer SELLER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "confirmed": true
  }'
```

## 📈 Статистика

```bash
curl "http://localhost:8000/campaigns/stats/my-stats" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Ответ для продавца:**
```json
{
  "total_campaigns": 15,
  "active_campaigns": 3,
  "completed_campaigns": 10,
  "pending_campaigns": 2,
  "total_spent": 50000.00
}
```

**Ответ для владельца канала:**
```json
{
  "total_campaigns": 25,
  "active_campaigns": 5,
  "completed_campaigns": 20,
  "pending_campaigns": 0,
  "total_earned": 125000.00
}
```

## 🔒 Безопасность

- Все endpoints требуют авторизации
- Проверка типа пользователя (seller/channel_owner)
- Проверка прав доступа (только создатель может редактировать)
- Проверка статуса перед изменением
- Логирование всех действий

## 📝 Логирование

Все действия по заявкам логируются в таблицу `campaign_activities`:
- `created` — создание заявки
- `accepted` — принятие заявки
- `rejected` — отклонение заявки
- `submitted` — отправка доказательства размещения
- `confirmed` — подтверждение размещения
- `disputed` — открытие спора
- `cancelled` — отмена заявки

## ✅ Готово к использованию

Модуль заявок полностью реализован и готов к использованию!

**Основные возможности:**
- ✅ Создание заявок с блокировкой средств
- ✅ Принятие/отклонение заявок
- ✅ Размещение рекламы с доказательствами
- ✅ Подтверждение и выплата
- ✅ Система споров
- ✅ Логирование всех действий
- ✅ Уведомления (логирование)
- ✅ Статистика по заявкам

