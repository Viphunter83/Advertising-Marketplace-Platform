# 🚀 Инструкция по запуску проекта

## ✅ Что было сделано

1. ✅ Создана полная структура проекта согласно спецификации
2. ✅ Настроена конфигурация приложения (`app/config.py`)
3. ✅ Создано подключение к Supabase (`app/core/database.py`)
4. ✅ Реализована система безопасности (JWT, хеширование паролей)
5. ✅ Созданы все SQLAlchemy модели (user, seller, channel, campaign, transaction, review, notification)
6. ✅ Созданы все Pydantic схемы для валидации API
7. ✅ Реализованы базовые эндпоинты аутентификации (регистрация, вход)
8. ✅ Созданы тестовые эндпоинты (GET /, GET /health)
9. ✅ Подготовлены SQL миграции для создания таблиц в Supabase

## 📋 Следующие шаги

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python3.10 -m venv venv

# Активируйте виртуальное окружение
# На Mac/Linux:
source venv/bin/activate
# На Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка Supabase

1. Создайте проект в [Supabase](https://supabase.com)
2. Получите следующие данные из настроек проекта:
   - `SUPABASE_URL` (Project URL)
   - `SUPABASE_KEY` (anon public key)
   - `SUPABASE_SERVICE_ROLE_KEY` (service_role key)

### 3. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp .env.example .env

# Откройте .env и заполните значения:
# - SUPABASE_URL
# - SUPABASE_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - JWT_SECRET_KEY (сгенерируйте случайную строку)
```

**Важно**: Никогда не коммитьте файл `.env` в Git!

### 4. Создание таблиц в Supabase

1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `supabase_migrations.sql`
4. Вставьте в SQL Editor и выполните (Run)

Это создаст все необходимые таблицы, индексы и триггеры.

### 5. Запуск приложения

```bash
# Убедитесь, что виртуальное окружение активировано
# Запустите сервер
uvicorn main:app --reload
```

Или напрямую через Python:

```bash
python main.py
```

### 6. Проверка работоспособности

1. Откройте браузер и перейдите на:
   - **API**: http://localhost:8000
   - **Swagger UI**: http://localhost:8000/docs
   - **ReDoc**: http://localhost:8000/redoc

2. Проверьте health endpoint:
   ```bash
   curl http://localhost:8000/health
   ```

   Должен вернуться ответ:
   ```json
   {
     "status": "healthy",
     "database": "connected",
     "app_name": "Advertising Marketplace MVP"
   }
   ```

## 🧪 Тестирование API

### Регистрация пользователя

```bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "full_name": "Test User",
    "user_type": "seller"
  }'
```

### Вход пользователя

```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

Ответ будет содержать `access_token`, который можно использовать для авторизованных запросов.

## 📁 Структура проекта

```
advertising-marketplace/
├── main.py                    # Точка входа
├── requirements.txt           # Зависимости
├── .env.example               # Пример переменных окружения
├── supabase_migrations.sql    # SQL миграции
├── README.md                  # Основная документация
├── SETUP.md                   # Эта инструкция
└── app/
    ├── config.py              # Конфигурация
    ├── core/                   # Безопасность, БД, зависимости
    ├── models/                 # SQLAlchemy модели
    ├── schemas/                # Pydantic схемы
    ├── services/               # Бизнес-логика (пока пусто)
    ├── routers/                # API маршруты
    └── utils/                  # Вспомогательные функции (пока пусто)
```

## ⚠️ Возможные проблемы

### Ошибка подключения к Supabase

- Проверьте, что переменные окружения в `.env` заполнены правильно
- Убедитесь, что таблицы созданы (выполнены миграции)
- Проверьте, что Supabase проект активен

### Ошибка импорта модулей

- Убедитесь, что виртуальное окружение активировано
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`

### Ошибка при создании пользователя

- Проверьте, что таблица `users` создана в Supabase
- Убедитесь, что используется правильный `SUPABASE_KEY` (не service_role для обычных операций)

## 🎯 Следующие задачи (Phase 1)

После успешного запуска можно переходить к следующим модулям:

1. Модуль управления профилями пользователей
2. Модуль каналов (создание, редактирование, фильтры)
3. Модуль заявок (создание, принятие, отклонение)
4. Модуль платежей и Escrow
5. Модуль уведомлений (email)
6. И другие...

## 📞 Поддержка

Если возникли проблемы, проверьте:
- Логи приложения в консоли
- Логи Supabase в Dashboard
- Swagger UI для тестирования API: http://localhost:8000/docs

